-- TODO: not used. see create_*.sql

/*
create table owner (
    id integer primary key,
    name unique not null,
    )

--http://stackoverflow.com/questions/3290183/managing-picture-tags-in-sql?rq=1
create table owner_node (
    id integer primary key,
    name unique not null,
    )

create table permission (
    id integer primary key,
    name unique not null,
    )

create table tag (
    id integer primary key,
    name unique not null,
    )
*/
